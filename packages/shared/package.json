{
  "name": "@rt-kanban/shared",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "lint": "echo \"(shared) no linting yet\"",
    "test": "echo \"(shared) no tests yet\""
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
